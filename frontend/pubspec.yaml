name: alertnest
description: Hackathon MVP Frontend for AlertNest
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  intl: ^0.18.1
  speech_to_text: ^6.6.0
  permission_handler: ^11.0.0
  avatar_glow: ^3.0.1
  record: ^6.0.0
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
